{
    "name": "mdbook-tools-dev",
    "dockerFile": "../Containerfile",
    "customizations": {
        "vscode": {
          "extensions": [
            "rust-lang.rust-analyzer",
            "tamasfe.even-better-toml"
          ]
        }
    }
    // "runArgs": [
    //     "--userns=keep-id:uid=$(id -u),gid=$(id -g)"
    // ]
}